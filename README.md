# notion4ever (patched fork)

## Чем этот форк отличается от оригинала

### 1. Always clean build

Перед каждым запуском:

* полностью удаляется `output_dir` (`_site`)
* данные скачиваются **всегда с нуля**

Нет неявного кеширования и «битых» состояний между запусками.

---

### 2. Поддержка нескольких root-страниц

Можно экспортировать несколько корневых страниц за один запуск:

```
-p page_id_1 -p page_id_2
```

или списком:

```
-p "id1,id2,id3"
```

Каждый root экспортируется в отдельную директорию.

---

### 3. Читаемые логи для больших пространств

Каждая строка логов имеет префикс текущей страницы:

```
[Дневник 🔽 1/6] INFO: HTTP Request: GET /blocks/...
```

---

## Структура результата

```
_site/
  root_<PAGE_ID>/
    notion_content.json        # raw dump (Notion API)
    notion_structured.json     # структурированные данные
    pages/                     # Markdown (ОСНОВНОЕ)
    assets/                    # файлы и изображения
    index.html                 # HTML (визуализация)
```

---

## Установка (локально)

```
git clone <URL_ТВОЕГО_ФОРКА>
cd notion4ever

python -m venv .venv
source .venv/bin/activate      # Windows: .venv\Scripts\activate

pip install -r requirements.txt
```

---

## Запуск

Минимальный запуск:

```
python -m notion4ever \
  -n YOUR_NOTION_TOKEN \
  -p YOUR_PAGE_ID
```

Несколько root-страниц:

```
python -m notion4ever \
  -n YOUR_NOTION_TOKEN \
  -p PAGE_ID_1 \
  -p PAGE_ID_2
```

---

## Важно знать

* Notion API ≈ **3 запроса в секунду**
* Большие пространства экспортируются **долго**
* 1–3 часа на полный бэкап — **нормально**

Если в логах идут запросы — процесс живой.

---

## Рекомендованный режим использования

* Использовать как **полный бэкап**
* Запускать **1 раз в неделю** или вручную
* Не пытаться делать частый incremental
